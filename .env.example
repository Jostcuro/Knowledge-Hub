PORT=
MONGODB_URI=
NODE_ENV=development

# === PRODUCTION (Render) ===
# Render asigna PORT automáticamente via variable de entorno.
# En Render Dashboard, configura estas variables:
#   NODE_ENV=production
#   MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/knowledge-hub
